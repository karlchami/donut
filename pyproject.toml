[tool.poetry]
name = "donut"
version = "0.1.0"
description = "it's a frikkin donut!"
authors = ["karljchami@gmail.com"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.92.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
